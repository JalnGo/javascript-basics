// Arrays - lists of things

var list = [ "dog", "cat", "horse" ];   // MEMORIZE

console.log(list[0]);   // should display which animal?
console.log(list[1]);   // should display which animal?
console.log(list[2]);   // should display which animal?


//// Encapsulate...
//function displayAnimals(animals) {
//    for (var i = 0; i < animals.length; i++) {    // use the 'length' property
//        console.log(animals[i]);
//    }
//}

//displayAnimals(list);
