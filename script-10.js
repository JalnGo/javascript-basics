// Determine if a number is between 1 and 20.

var number = 10;  // should be true
//var number = 0;   // should be false
//var number = 20;  // should be false

if (number > 0) {
    if (number < 20) {
        console.log("the number is between 1 and 20");
    }
}

//if (number > 0 && number < 20) {      // KEY POINT
//    console.log("the number is between 1 and 20");
//}
